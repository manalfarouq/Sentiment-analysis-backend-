# .env.example — Variables d'environnement pour l'application

#* JWT (JSON Web Token) Configuration
# Secret key pour signer les tokens JWT
# IMPORTANT : Utilise une clé longue et aléatoire en production
# Génère-la avec : openssl rand -hex 32
SK=your_secret_key_here_change_this_in_production

#* Algorithme de cryptage pour JWT (ne pas changer)
ALG=HS256

#* Durée de validité du token en minutes (60 = 1 heure)
ACCESS_TOKEN_EXPIRE_MINUTES=60


#* HuggingFace API
# Token API HuggingFace pour l'analyse de sentiment
# Obtiens-le sur : https://huggingface.co/settings/tokens
HF_API_TOKEN=your_huggingface_token_here


#* PostgreSQL Database Configuration
# Adresse du serveur PostgreSQL (localhost pour développement local)
DB_HOST=localhost

#* Port PostgreSQL (5432 par défaut)
DB_PORT=5432

#* Nom de la base de données
DB_NAME=sentiment_db

#* Nom d'utilisateur PostgreSQL
DB_USER=your_db_username

#* Mot de passe PostgreSQL
DB_PASSWORD=your_db_password


#* URL complète pour SQLAlchemy (optionnel si tu l'utilises plus tard)
# Format: postgresql://username:password@host:port/database
# DATABASE_URL=postgresql://your_db_username:your_db_password@localhost:5432/sentiment_db